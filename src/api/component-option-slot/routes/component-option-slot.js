'use strict';

/**
 * component-option-slot router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::component-option-slot.component-option-slot');
