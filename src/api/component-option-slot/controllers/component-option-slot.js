'use strict';

/**
 * component-option-slot controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::component-option-slot.component-option-slot');
